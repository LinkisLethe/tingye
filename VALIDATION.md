# 验证范围

本地 Windows 环境的 54 项自动化测试通过。可运行以下命令复现，GitHub Actions 使用同一测试入口。

```powershell
python -m pip install -r requirements.txt
python -m unittest discover -s tests -v
```

| 模块 | 检查内容 |
| --- | --- |
| 媒体获取 | 链接与分 P 解析、字幕读取、音轨下载、取消、重定向目标限制、不完整下载处理 |
| 语音识别 | 取消检查、漏段候选、补转预算、重复识别过滤、时间戳与序列化 |
| 笔记导出 | 剪藏字段、Markdown 单文件输出、同视频覆盖、跨子目录副本匹配、备份和并发写入 |
| 队列 | 设置快照、取消、重试、中断恢复、旧任务结果隔离和远端任务状态保留 |
| 本地服务 | 页面与静态文件、本地令牌、Host 与 Origin 检查 |

测试在临时目录中创建笔记库，媒体网络请求与模型推理使用模拟响应。通过测试说明上述处理与保存行为符合断言，不等于验证了当前 B 站接口可用性、远端 GPU 部署或逐句转写准确率。

## 手动验证方法

在单独的测试笔记库中分别收录有字幕和无字幕的视频，检查来源字段、时间戳和文件位置。再次提交同一 BV 与分 P，确认目标目录只剩一份对应 Markdown，且笔记库外生成覆盖备份。

尝试取消任务、关闭网页后重新打开，以及重启服务后恢复中断任务。音轨应在任务结束后从应用工作目录清理，不应进入笔记库。

长视频、多人混合语音和专有名词可能出现识别错误。准确率需要另行使用人工参考文本评估。
